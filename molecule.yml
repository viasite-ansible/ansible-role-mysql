---
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-mysql-1604
      image: ubuntu
      image_version: 16.04
      volume_mounts:
        - ${MOLECULE_APTPROXY_PATH}:/etc/apt/apt.conf.d/00aptproxy

#driver:
#  name: vagrant
#vagrant:
#  platforms:
#    - name: xenial64
#      box: bento/ubuntu-16.04
#  providers:
#    - name: virtualbox
#      type: virtualbox
#  instances:
#    - name: vagrant-01

ansible:
  playbook: tests/playbook.yml
  group_vars:
    all:
      mysql_innodb_log_file_size: ${MOLECULE_MYSQL_INNODB_LOG_FILE_SIZE}
